(import test/lists)

(= 2 (- 3 1))

(defn ++ [x] (+ x 1))

(defn ->> [x f ...rest] 
    (if (nil? rest)
        (f x)
        (cons (f x) rest)))

(->> 2
    ++
    (fn [x] (= 3 x)))
(->> 2
    ++
    ++
    ++)

(list (+ 1 2) 4 () (fn [] ()))

(pairs (range 0 10))